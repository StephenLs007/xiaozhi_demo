{
    "target": "esp32c3",   // 目标芯片型号
    "builds": [
        {
            "name": "mucit-c3-0.96oled-wifi",   // 开发板名称
            "sdkconfig_append": [
                
                // 额外需要的编译配置
                "CONFIG_PM_ENABLE=y",
                "CONFIG_FREERTOS_USE_TICKLESS_IDLE=y"
               // "CONFIG_ESPTOOLPY_FLASHSIZE_4MB=y",
               // "CONFIG_PARTITION_TABLE_CUSTOM_FILENAME=\"partitions_4M.csv\""
            ]
        }
    ]
}
